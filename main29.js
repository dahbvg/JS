
let firstNumer = 5;

let secondNumber = 14;

function suma(firstNumer, secondNumber) {
    let wynik = firstNumer + secondNumber;

    return wynik;
}

console.log(suma (firstNumer, secondNumber));

let dodowanie = suma(firstNumer, secondNumber);

console.log(dodowanie);
